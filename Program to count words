//Program to count the words.

#include <iostream>

using namespace std;

int main()
{
    string str= "How are you";
    int countwords=1;
    int i;
    for(i=0; str[i]!='\0'; i++){
        if(str[i]==' '  && str[i-1]!=' '){
            countwords++;
        }
    }
    cout<<countwords<<endl;
}
